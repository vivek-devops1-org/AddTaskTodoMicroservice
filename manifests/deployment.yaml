apiVersion: apps/v1
kind: Deployment
metadata:
  name: add-task-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: add-task
  template:
    metadata:
      name: template-pod
      labels:
        app: add-task
    spec:
      containers:
      - name: add-task-container
        image: todoacr001.azurecr.io/addtask:v10
        ports:
        - containerPort: 8000
