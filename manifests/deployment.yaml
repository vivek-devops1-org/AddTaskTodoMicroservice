apiVersion: apps/v1
kind: Deployment
metadata:
  name: add-task-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: add-task
  template:
    metadata:
      name: template-pod
      labels:
        app: add-task
    spec:
      containers:
      - name: add-task-container
        image: practice30.azurecr.io/addtask:v6
        ports:
        - containerPort: 8000
